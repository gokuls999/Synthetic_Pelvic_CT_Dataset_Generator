numpy>=1.24
scipy>=1.10
pandas>=2.0
PyYAML>=6.0
scikit-learn>=1.3
scikit-image>=0.21
matplotlib>=3.7
Pillow>=10.0

pydicom>=2.4
nibabel>=5.1
SimpleITK>=2.3
fpdf2>=2.7

torch>=2.1
torchvision>=0.16
einops>=0.7

# Optional: anatomy validation (TotalSegmentator). Install ONLY when ready to
# validate; pulls ~2 GB of model weights on first use.
# pip install totalsegmentator
